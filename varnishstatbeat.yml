##################### Varnishbeat Configuration Example #######################

output:
  elasticsearch:
    hosts: ["localhost:9200"]
    template.enabled: false
    index: "varnishstatbeat"

logging:
    level: "debug"
